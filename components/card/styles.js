import { StyleSheet } from "react-native";

const stylesCard = StyleSheet.create({
  container: {
    flex: 1,
    backgroundColor: "#fff",
    justifyContent: "center",
    padding: 20,
    margin: 10,
  },
});

export {stylesHome}